# Frontier Lattice — Environment Variables
# Copy to .env and fill in values:  cp .env.example .env

# ── Sui Network ────────────────────────────────────────────────────
SUI_RPC_URL=http://127.0.0.1:9000
# SUI_WS_URL=ws://127.0.0.1:9000

# ── Deployed Package IDs (set after `sui client publish`) ──────────
PACKAGE_TRIBE=
PACKAGE_CONTRACT_BOARD=
PACKAGE_FORGE_PLANNER=
PACKAGE_TRUSTLESS_CONTRACTS=

# ── Web App (Vite) ─────────────────────────────────────────────────
VITE_TRUSTLESS_CONTRACTS_PACKAGE_ID=

# ── Database ───────────────────────────────────────────────────────
DATABASE_URL=postgresql://lattice:lattice@localhost:5432/frontier_lattice

# ── Indexer ────────────────────────────────────────────────────────
INDEXER_API_URL=http://localhost:3100
API_PORT=3100
POLL_INTERVAL_MS=2000

# ── App API ────────────────────────────────────────────────────────
# SESSION_SECRET=changeme
# S3_BUCKET_ENCRYPTED=frontier-lattice-encrypted

# ── AWS (for deployment scripts) ───────────────────────────────────
AWS_REGION=us-east-1
# AWS_ACCOUNT_ID=
