# Corm Brain — Isolated Test Harness (mprocs)
#
# Runs only the components needed to test corm-brain with LLM responses,
# isolated from the puzzle-service and the rest of the stack.
#
# Install:  cargo install mprocs   (or: brew install mprocs)
# Run:      cd corm-brain && mprocs
#
# Keybindings (default):
#   q        quit all
#   j/k      switch process
#   x        stop/restart focused process
#   Enter    toggle focus on log pane

procs:
  # ── Postgres (via docker compose in parent dir) ──────────────────
  postgres:
    shell: |
      trap 'docker compose -f ../docker-compose.yml down postgres' EXIT
      docker compose -f ../docker-compose.yml up postgres
    stop: "SIGTERM"

  # ── DGX Spark — SSH Tunnel (forwards model ports to localhost) ───
  dgx-tunnel:
    shell: "bash ../scripts/dgx-tunnel.sh"
    stop: "SIGINT"

  # ── Test Harness (impersonates puzzle-service on :3300) ──────────
  harness:
    shell: |
      echo "Waiting for postgres..."
      until docker exec frontier-corm-postgres-1 pg_isready -U corm -d frontier_corm -q 2>/dev/null; do
        sleep 1
      done
      echo "Postgres ready — starting harness"
      bash scripts/harness.sh
    env:
      HARNESS_PORT: "3300"
    stop: "SIGTERM"

  # ── Corm Brain (connects to harness on :3300) ───────────────────
  corm-brain:
    shell: |
      echo "Waiting for postgres..."
      until docker exec frontier-corm-postgres-1 pg_isready -U corm -d frontier_corm -q 2>/dev/null; do
        sleep 1
      done
      echo "Waiting for harness on :3300..."
      until curl -sf http://localhost:3300/health >/dev/null 2>&1; do
        sleep 1
      done
      echo "Waiting for TRT-LLM tunnel (Nano on :8001)..."
      until curl -sf http://localhost:8001/health >/dev/null 2>&1; do
        sleep 3
      done
      echo "Checking TRT-LLM tunnel (Super on :8000)..."
      SUPER_TRIES=0
      while [ $SUPER_TRIES -lt 10 ] && ! curl -sf http://localhost:8000/health >/dev/null 2>&1; do
        SUPER_TRIES=$((SUPER_TRIES + 1))
        sleep 3
      done
      if curl -sf http://localhost:8000/health >/dev/null 2>&1; then
        echo "Super is healthy."
      else
        echo "WARN: Super not available — corm-brain will use Nano only."
      fi
      echo "Dependencies ready — starting corm-brain (air live-reload)"
      air
    env:
      LLM_SUPER_URL: "http://localhost:8000"
      LLM_FAST_URL: "http://localhost:8001"
      EMBED_MODEL_PATH: "../models/nomic-embed"
      DATABASE_URL: "postgresql://corm:corm@localhost:5432/frontier_corm"
      PUZZLE_SERVICE_URL: "http://localhost:3300"
      SUI_RPC_URL: "http://127.0.0.1:9000"
      CONSOLIDATION_INTERVAL_MS: "60000"
      MEMORY_CAP_PER_CORM: "500"
      EVENT_COALESCE_MS: "50"
      WS_RECONNECT_MAX_MS: "30000"
      FALLBACK_POLL_INTERVAL_MS: "2000"
    stop: "SIGTERM"
