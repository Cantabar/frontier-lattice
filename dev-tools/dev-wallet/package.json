{
  "name": "frontier-dev-wallet",
  "version": "0.1.0",
  "private": true,
  "description": "Localnet dev wallet for Frontier Lattice",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "@mysten/sui": "^1.45.2"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
