{
  "name": "@frontier-lattice/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "synth": "cdk synth",
    "deploy": "cdk deploy --all --require-approval never",
    "destroy": "cdk destroy --all --force"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.180.0",
    "constructs": "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.0",
    "aws-cdk": "^2.180.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.7.0"
  }
}
