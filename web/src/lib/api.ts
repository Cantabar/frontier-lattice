/**
 * Indexer API re-exports.
 *
 * All consumers should import from this module instead of `indexer.ts`
 * directly so that the import path stays stable if the underlying
 * implementation ever changes.
 */

export * from "./indexer";
